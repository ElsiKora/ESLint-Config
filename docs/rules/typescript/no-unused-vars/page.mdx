import { Callout, Tabs } from "nextra/components";

# `@elsikora/typescript/no-unused-vars`

<Callout type="info" emoji="🧭">
	**Disabled.** ElsiKora includes this rule as an intentional disabled override in at least one preset scenario.
</Callout>

## Rule Overview

Disallow unused variables.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value         |
| ------------------ | ------------- |
| Type               | `problem`     |
| Fixable            | ✅ Yes        |
| Has suggestions    | ✅ Yes        |
| Deprecated         | ❌ No         |
| ESLint recommended | `recommended` |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
[
	{
		"oneOf": [
			{
				"type": "string",
				"description": "Broad setting for unused variables to target.",
				"enum": ["all", "local"]
			},
			{
				"type": "object",
				"additionalProperties": false,
				"properties": {
					"args": {
						"type": "string",
						"description": "Whether to check all, some, or no arguments.",
						"enum": ["all", "after-used", "none"]
					},
					"argsIgnorePattern": {
						"type": "string",
						"description": "Regular expressions of argument names to not check for usage."
					},
					"caughtErrors": {
						"type": "string",
						"description": "Whether to check catch block arguments.",
						"enum": ["all", "none"]
					},
					"caughtErrorsIgnorePattern": {
						"type": "string",
						"description": "Regular expressions of catch block argument names to not check for usage."
					},
					"destructuredArrayIgnorePattern": {
						"type": "string",
						"description": "Regular expressions of destructured array variable names to not check for usage."
					},
					"enableAutofixRemoval": {
						"type": "object",
						"additionalProperties": false,
						"description": "Configurable automatic fixes for different types of unused variables.",
						"properties": {
							"imports": {
								"type": "boolean",
								"description": "Whether to enable automatic removal of unused imports."
							}
						}
					},
					"ignoreClassWithStaticInitBlock": {
						"type": "boolean",
						"description": "Whether to ignore classes with at least one static initialization block."
					},
					"ignoreRestSiblings": {
						"type": "boolean",
						"description": "Whether to ignore sibling properties in `...` destructurings."
					},
					"ignoreUsingDeclarations": {
						"type": "boolean",
						"description": "Whether to ignore using or await using declarations."
					},
					"reportUsedIgnorePattern": {
						"type": "boolean",
						"description": "Whether to report variables that match any of the valid ignore pattern options if they have been used."
					},
					"vars": {
						"type": "string",
						"description": "Whether to check all variables or only locally-declared variables.",
						"enum": ["all", "local"]
					},
					"varsIgnorePattern": {
						"type": "string",
						"description": "Regular expressions of variable names to not check for usage."
					}
				}
			}
		]
	}
]
```

Upstream default options:

```json filename="default-options.json" copy
[{}]
```

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                           |
| ---------------- | ------------------------------- |
| Default scenario | `all-options`                   |
| Default target   | `TypeScript` (`src/example.ts`) |
| Default severity | 🧭 `off`                        |
| Default value    | `[0]`                           |

### Effective Result by File Type

| Scenario                         | Target       | File path                 | Effective severity | Effective value |
| -------------------------------- | ------------ | ------------------------- | ------------------ | --------------- |
| `typescript`                     | `TypeScript` | `src/example.ts`          | 🧭 `off`           | `[0]`           |
| `typescript`                     | `TSX`        | `src/example.tsx`         | 🧭 `off`           | `[0]`           |
| `typescript`                     | `Next app`   | `app/page.tsx`            | 🧭 `off`           | `[0]`           |
| `typescript`                     | `Storybook`  | `src/example.stories.tsx` | 🧭 `off`           | `[0]`           |
| `typescript-strict`              | `TypeScript` | `src/example.ts`          | 🧭 `off`           | `[0]`           |
| `typescript-strict`              | `TSX`        | `src/example.tsx`         | 🧭 `off`           | `[0]`           |
| `typescript-strict`              | `Next app`   | `app/page.tsx`            | 🧭 `off`           | `[0]`           |
| `typescript-strict`              | `Storybook`  | `src/example.stories.tsx` | 🧭 `off`           | `[0]`           |
| `typescript-with-unicorn`        | `TypeScript` | `src/example.ts`          | 🧭 `off`           | `[0]`           |
| `typescript-with-unicorn`        | `TSX`        | `src/example.tsx`         | 🧭 `off`           | `[0]`           |
| `typescript-with-unicorn`        | `Next app`   | `app/page.tsx`            | 🧭 `off`           | `[0]`           |
| `typescript-with-unicorn`        | `Storybook`  | `src/example.stories.tsx` | 🧭 `off`           | `[0]`           |
| `typescript-strict-with-unicorn` | `TypeScript` | `src/example.ts`          | 🧭 `off`           | `[0]`           |
| `typescript-strict-with-unicorn` | `TSX`        | `src/example.tsx`         | 🧭 `off`           | `[0]`           |
| `typescript-strict-with-unicorn` | `Next app`   | `app/page.tsx`            | 🧭 `off`           | `[0]`           |
| `typescript-strict-with-unicorn` | `Storybook`  | `src/example.stories.tsx` | 🧭 `off`           | `[0]`           |
| `all-options`                    | `TypeScript` | `src/example.ts`          | 🧭 `off`           | `[0]`           |
| `all-options`                    | `TSX`        | `src/example.tsx`         | 🧭 `off`           | `[0]`           |
| `all-options`                    | `Next app`   | `app/page.tsx`            | 🧭 `off`           | `[0]`           |
| `all-options`                    | `Storybook`  | `src/example.stories.tsx` | 🧭 `off`           | `[0]`           |

### Configured Entries Before ESLint Resolves Overrides

| Scenario                         | Severity   | Value     | Config block |
| -------------------------------- | ---------- | --------- | ------------ |
| `typescript`                     | 🚨 `error` | `"error"` | `2`          |
| `typescript`                     | 🚨 `error` | `"error"` | `5`          |
| `typescript`                     | 🧭 `off`   | `"off"`   | `9`          |
| `typescript-strict`              | 🚨 `error` | `"error"` | `2`          |
| `typescript-strict`              | 🚨 `error` | `"error"` | `5`          |
| `typescript-strict`              | 🧭 `off`   | `"off"`   | `9`          |
| `typescript-with-unicorn`        | 🚨 `error` | `"error"` | `2`          |
| `typescript-with-unicorn`        | 🚨 `error` | `"error"` | `5`          |
| `typescript-with-unicorn`        | 🧭 `off`   | `"off"`   | `9`          |
| `typescript-strict-with-unicorn` | 🚨 `error` | `"error"` | `2`          |
| `typescript-strict-with-unicorn` | 🚨 `error` | `"error"` | `5`          |
| `typescript-strict-with-unicorn` | 🧭 `off`   | `"off"`   | `9`          |
| `all-options`                    | 🚨 `error` | `"error"` | `57`         |
| `all-options`                    | 🚨 `error` | `"error"` | `60`         |
| `all-options`                    | 🧭 `off`   | `"off"`   | `64`         |
| `all-options`                    | 🚨 `error` | `"error"` | `67`         |
| `all-options`                    | 🚨 `error` | `"error"` | `70`         |
| `all-options`                    | 🧭 `off`   | `"off"`   | `74`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```ts filename="bad-example.ts" copy
class UserService {
	constructor(private readonly repository: UserRepository) {}

	load(id: string): Promise<User> {
		return this.repository.findById(id);
	}
}

export { UserService };
```

  </Tabs.Tab>
  <Tabs.Tab>

```ts filename="good-example.ts" copy
class UserService {
	constructor(private readonly repository: UserRepository) {}

	loadUser(id: string): Promise<User> {
		return this.repository.findById(id);
	}
}

export { UserService };
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/typescript/no-unused-vars`
- **Documentation route:** `/docs/eslint-config/rules/typescript/no-unused-vars`
