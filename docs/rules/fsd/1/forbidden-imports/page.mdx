import { Callout, Tabs } from "nextra/components";

# `@elsikora/fsd/1/forbidden-imports`

<Callout type="error" emoji="🚨">
	**Error.** ElsiKora reports this rule as an error in at least one preset scenario.
</Callout>

## Rule Overview

Prevents imports from higher layers and cross-imports between slices.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value     |
| ------------------ | --------- |
| Type               | `problem` |
| Fixable            | ❌ No     |
| Has suggestions    | ❌ No     |
| Deprecated         | ❌ No     |
| ESLint recommended | `true`    |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
[
	{
		"type": "object",
		"properties": {
			"rootPath": {
				"type": "string"
			},
			"tsconfigPath": {
				"type": "string"
			},
			"alias": {
				"oneOf": [
					{
						"type": "string"
					},
					{
						"type": "object",
						"properties": {
							"value": {
								"type": "string"
							},
							"withSlash": {
								"type": "boolean"
							}
						},
						"required": ["value"],
						"additionalProperties": false
					}
				]
			},
			"layers": {
				"type": "object",
				"additionalProperties": {
					"type": "object",
					"properties": {
						"pattern": {
							"type": "string"
						},
						"priority": {
							"type": "number"
						},
						"allowedToImport": {
							"type": "array",
							"items": {
								"type": "string"
							}
						}
					},
					"additionalProperties": false
				}
			},
			"folderPattern": {
				"type": "object",
				"properties": {
					"enabled": {
						"type": "boolean"
					},
					"regex": {
						"type": "string"
					},
					"extractionGroup": {
						"type": "number"
					}
				},
				"additionalProperties": false
			},
			"testFilesPatterns": {
				"type": "array",
				"items": {
					"type": "string"
				}
			},
			"ignoreImportPatterns": {
				"type": "array",
				"items": {
					"type": "string"
				}
			}
		},
		"additionalProperties": false
	}
]
```

No upstream default options are exposed in the rule metadata.

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                           |
| ---------------- | ------------------------------- |
| Default scenario | `all-options`                   |
| Default target   | `JavaScript` (`src/example.js`) |
| Default severity | 🚨 `error`                      |
| Default value    | `[2]`                           |

### Effective Result by File Type

| Scenario                 | Target         | File path                 | Effective severity | Effective value |
| ------------------------ | -------------- | ------------------------- | ------------------ | --------------- |
| `fsd`                    | `JavaScript`   | `src/example.js`          | 🚨 `error`         | `[2]`           |
| `fsd`                    | `JSX`          | `src/example.jsx`         | 🚨 `error`         | `[2]`           |
| `fsd`                    | `TypeScript`   | `src/example.ts`          | 🚨 `error`         | `[2]`           |
| `fsd`                    | `TSX`          | `src/example.tsx`         | 🚨 `error`         | `[2]`           |
| `fsd`                    | `Next pages`   | `pages/index.jsx`         | 🚨 `error`         | `[2]`           |
| `fsd`                    | `Next app`     | `app/page.tsx`            | 🚨 `error`         | `[2]`           |
| `fsd`                    | `Storybook`    | `src/example.stories.tsx` | 🚨 `error`         | `[2]`           |
| `perfectionist-with-fsd` | `JavaScript`   | `src/example.js`          | 🚨 `error`         | `[2]`           |
| `perfectionist-with-fsd` | `JSX`          | `src/example.jsx`         | 🚨 `error`         | `[2]`           |
| `perfectionist-with-fsd` | `TypeScript`   | `src/example.ts`          | 🚨 `error`         | `[2]`           |
| `perfectionist-with-fsd` | `TSX`          | `src/example.tsx`         | 🚨 `error`         | `[2]`           |
| `perfectionist-with-fsd` | `Next pages`   | `pages/index.jsx`         | 🚨 `error`         | `[2]`           |
| `perfectionist-with-fsd` | `Next app`     | `app/page.tsx`            | 🚨 `error`         | `[2]`           |
| `perfectionist-with-fsd` | `Storybook`    | `src/example.stories.tsx` | 🚨 `error`         | `[2]`           |
| `all-options`            | `JavaScript`   | `src/example.js`          | 🚨 `error`         | `[2]`           |
| `all-options`            | `JSX`          | `src/example.jsx`         | 🚨 `error`         | `[2]`           |
| `all-options`            | `TypeScript`   | `src/example.ts`          | 🚨 `error`         | `[2]`           |
| `all-options`            | `TSX`          | `src/example.tsx`         | 🚨 `error`         | `[2]`           |
| `all-options`            | `Next pages`   | `pages/index.jsx`         | 🚨 `error`         | `[2]`           |
| `all-options`            | `Next app`     | `app/page.tsx`            | 🚨 `error`         | `[2]`           |
| `all-options`            | `Storybook`    | `src/example.stories.tsx` | 🚨 `error`         | `[2]`           |
| `all-options`            | `Package JSON` | `package.json`            | 🚨 `error`         | `[2]`           |
| `all-options`            | `JSON`         | `config.json`             | 🚨 `error`         | `[2]`           |
| `all-options`            | `YAML`         | `config.yaml`             | 🚨 `error`         | `[2]`           |
| `all-options`            | `Markdown`     | `README.md`               | 🚨 `error`         | `[2]`           |
| `all-options`            | `CSS`          | `src/styles.css`          | 🚨 `error`         | `[2]`           |

### Configured Entries Before ESLint Resolves Overrides

| Scenario                 | Severity   | Value     | Config block |
| ------------------------ | ---------- | --------- | ------------ |
| `fsd`                    | 🚨 `error` | `"error"` | `0`          |
| `perfectionist-with-fsd` | 🚨 `error` | `"error"` | `0`          |
| `all-options`            | 🚨 `error` | `"error"` | `3`          |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```ts filename="bad-example.ts" copy
import { checkoutModel } from "@/features/checkout/model/private-model";
```

  </Tabs.Tab>
  <Tabs.Tab>

```ts filename="good-example.ts" copy
import { checkoutActions } from "@/features/checkout";
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/fsd/1/forbidden-imports`
- **Documentation route:** `/docs/eslint-config/rules/fsd/1/forbidden-imports`
