import { Callout, Tabs } from "nextra/components";

# `@elsikora/unicorn/import-style`

<Callout type="error" emoji="🚨">
	**Error.** ElsiKora reports this rule as an error in at least one preset scenario.
</Callout>

## Rule Overview

Enforce specific import styles per module.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value           |
| ------------------ | --------------- |
| Type               | `problem`       |
| Fixable            | ❌ No           |
| Has suggestions    | ❌ No           |
| Deprecated         | ❌ No           |
| ESLint recommended | `unopinionated` |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
{
	"type": "array",
	"additionalItems": false,
	"items": [
		{
			"type": "object",
			"additionalProperties": false,
			"properties": {
				"checkImport": {
					"type": "boolean",
					"description": "Whether to check `import` statements."
				},
				"checkDynamicImport": {
					"type": "boolean",
					"description": "Whether to check dynamic `import()` expressions."
				},
				"checkExportFrom": {
					"type": "boolean",
					"description": "Whether to check `export … from` statements."
				},
				"checkRequire": {
					"type": "boolean",
					"description": "Whether to check `require()` calls."
				},
				"extendDefaultStyles": {
					"type": "boolean",
					"description": "Whether to extend the default styles."
				},
				"styles": {
					"$ref": "#/definitions/moduleStyles",
					"description": "Module import styles."
				}
			}
		}
	],
	"definitions": {
		"moduleStyles": {
			"type": "object",
			"additionalProperties": {
				"$ref": "#/definitions/styles"
			}
		},
		"styles": {
			"anyOf": [
				{
					"enum": [false]
				},
				{
					"$ref": "#/definitions/booleanObject"
				}
			]
		},
		"booleanObject": {
			"type": "object",
			"additionalProperties": {
				"type": "boolean"
			}
		}
	}
}
```

No upstream default options are exposed in the rule metadata.

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                           |
| ---------------- | ------------------------------- |
| Default scenario | `all-options`                   |
| Default target   | `JavaScript` (`src/example.js`) |
| Default severity | 🚨 `error`                      |
| Default value    | `[2,{}]`                        |

### Effective Result by File Type

| Scenario                         | Target       | File path                 | Effective severity | Effective value |
| -------------------------------- | ------------ | ------------------------- | ------------------ | --------------- |
| `unicorn`                        | `JavaScript` | `src/example.js`          | 🚨 `error`         | `[2,{}]`        |
| `unicorn`                        | `JSX`        | `src/example.jsx`         | 🚨 `error`         | `[2,{}]`        |
| `unicorn`                        | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{}]`        |
| `unicorn`                        | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{}]`        |
| `unicorn`                        | `Next pages` | `pages/index.jsx`         | 🚨 `error`         | `[2,{}]`        |
| `unicorn`                        | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{}]`        |
| `unicorn`                        | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{}]`        |
| `node-with-unicorn`              | `JavaScript` | `src/example.js`          | 🚨 `error`         | `[2,{}]`        |
| `node-with-unicorn`              | `JSX`        | `src/example.jsx`         | 🚨 `error`         | `[2,{}]`        |
| `node-with-unicorn`              | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{}]`        |
| `node-with-unicorn`              | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{}]`        |
| `node-with-unicorn`              | `Next pages` | `pages/index.jsx`         | 🚨 `error`         | `[2,{}]`        |
| `node-with-unicorn`              | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{}]`        |
| `node-with-unicorn`              | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{}]`        |
| `typescript-with-unicorn`        | `JavaScript` | `src/example.js`          | 🚨 `error`         | `[2,{}]`        |
| `typescript-with-unicorn`        | `JSX`        | `src/example.jsx`         | 🚨 `error`         | `[2,{}]`        |
| `typescript-with-unicorn`        | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{}]`        |
| `typescript-with-unicorn`        | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{}]`        |
| `typescript-with-unicorn`        | `Next pages` | `pages/index.jsx`         | 🚨 `error`         | `[2,{}]`        |
| `typescript-with-unicorn`        | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{}]`        |
| `typescript-with-unicorn`        | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{}]`        |
| `typescript-strict-with-unicorn` | `JavaScript` | `src/example.js`          | 🚨 `error`         | `[2,{}]`        |
| `typescript-strict-with-unicorn` | `JSX`        | `src/example.jsx`         | 🚨 `error`         | `[2,{}]`        |
| `typescript-strict-with-unicorn` | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{}]`        |
| `typescript-strict-with-unicorn` | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{}]`        |
| `typescript-strict-with-unicorn` | `Next pages` | `pages/index.jsx`         | 🚨 `error`         | `[2,{}]`        |
| `typescript-strict-with-unicorn` | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{}]`        |
| `typescript-strict-with-unicorn` | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{}]`        |
| `all-options`                    | `JavaScript` | `src/example.js`          | 🚨 `error`         | `[2,{}]`        |
| `all-options`                    | `JSX`        | `src/example.jsx`         | 🚨 `error`         | `[2,{}]`        |
| `all-options`                    | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{}]`        |
| `all-options`                    | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{}]`        |
| `all-options`                    | `Next pages` | `pages/index.jsx`         | 🚨 `error`         | `[2,{}]`        |
| `all-options`                    | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{}]`        |
| `all-options`                    | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{}]`        |

### Configured Entries Before ESLint Resolves Overrides

| Scenario                         | Severity   | Value     | Config block |
| -------------------------------- | ---------- | --------- | ------------ |
| `unicorn`                        | 🚨 `error` | `"error"` | `0`          |
| `node-with-unicorn`              | 🚨 `error` | `"error"` | `2`          |
| `typescript-with-unicorn`        | 🚨 `error` | `"error"` | `10`         |
| `typescript-strict-with-unicorn` | 🚨 `error` | `"error"` | `10`         |
| `all-options`                    | 🚨 `error` | `"error"` | `75`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```js filename="bad-example.js" copy
const fs = require("fs");
module.exports = createServer;
```

  </Tabs.Tab>
  <Tabs.Tab>

```js filename="good-example.js" copy
import fs from "node:fs";
export { createServer };
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/unicorn/import-style`
- **Documentation route:** `/docs/eslint-config/rules/unicorn/import-style`
