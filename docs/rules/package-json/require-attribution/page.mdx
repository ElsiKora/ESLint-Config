import { Callout, Tabs } from "nextra/components";

# `@elsikora/package-json/require-attribution`

<Callout type="error" emoji="🚨">
	**Error.** ElsiKora reports this rule as an error in at least one preset scenario.
</Callout>

## Rule Overview

Ensures that proper attribution is included, requiring that either `author` or `contributors` is defined, and that if `contributors` is present, it should include at least one contributor.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value        |
| ------------------ | ------------ |
| Type               | `suggestion` |
| Fixable            | ❌ No        |
| Has suggestions    | ✅ Yes       |
| Deprecated         | ❌ No        |
| ESLint recommended | `true`       |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
[
	{
		"additionalProperties": false,
		"properties": {
			"ignorePrivate": {
				"description": "Skip attribution requirements for packages with `\"private\": true`.",
				"type": "boolean"
			},
			"preferContributorsOnly": {
				"description": "Require that only `contributors` is present, and `author` is not defined.",
				"type": "boolean"
			}
		},
		"type": "object"
	}
]
```

No upstream default options are exposed in the rule metadata.

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                                                       |
| ---------------- | ----------------------------------------------------------- |
| Default scenario | `all-options`                                               |
| Default target   | `Package JSON` (`package.json`)                             |
| Default severity | 🚨 `error`                                                  |
| Default value    | `[2,{"ignorePrivate":true,"preferContributorsOnly":false}]` |

### Effective Result by File Type

| Scenario       | Target         | File path      | Effective severity | Effective value                                             |
| -------------- | -------------- | -------------- | ------------------ | ----------------------------------------------------------- |
| `package-json` | `Package JSON` | `package.json` | 🚨 `error`         | `[2,{"ignorePrivate":true,"preferContributorsOnly":false}]` |
| `all-options`  | `Package JSON` | `package.json` | 🚨 `error`         | `[2,{"ignorePrivate":true,"preferContributorsOnly":false}]` |

### Configured Entries Before ESLint Resolves Overrides

| Scenario       | Severity   | Value     | Config block |
| -------------- | ---------- | --------- | ------------ |
| `package-json` | 🚨 `error` | `"error"` | `0`          |
| `all-options`  | 🚨 `error` | `"error"` | `29`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```json filename="bad-example.json" copy
{
	"name": "example-package"
}
```

  </Tabs.Tab>
  <Tabs.Tab>

```json filename="good-example.json" copy
{
	"name": "example-package",
	"attribution": "TODO"
}
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/package-json/require-attribution`
- **Documentation route:** `/docs/eslint-config/rules/package-json/require-attribution`
