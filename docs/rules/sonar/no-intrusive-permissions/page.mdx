import { Callout, Tabs } from "nextra/components";

# `@elsikora/sonar/no-intrusive-permissions`

<Callout type="error" emoji="🚨">
	**Error.** ElsiKora reports this rule as an error in at least one preset scenario.
</Callout>

## Rule Overview

Using intrusive permissions is security-sensitive.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value     |
| ------------------ | --------- |
| Type               | `problem` |
| Fixable            | ❌ No     |
| Has suggestions    | ❌ No     |
| Deprecated         | ❌ No     |
| ESLint recommended | `true`    |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
{
	"type": "array",
	"minItems": 0,
	"maxItems": 1,
	"items": [
		{
			"type": "object",
			"properties": {
				"permissions": {
					"type": "array",
					"items": {
						"type": "string"
					}
				}
			},
			"additionalProperties": false
		}
	]
}
```

No upstream default options are exposed in the rule metadata.

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                                 |
| ---------------- | ------------------------------------- |
| Default scenario | `all-options`                         |
| Default target   | `JavaScript` (`src/example.js`)       |
| Default severity | 🚨 `error`                            |
| Default value    | `[2,{"permissions":["geolocation"]}]` |

### Effective Result by File Type

| Scenario                | Target       | File path                 | Effective severity | Effective value                       |
| ----------------------- | ------------ | ------------------------- | ------------------ | ------------------------------------- |
| `sonar`                 | `JavaScript` | `src/example.js`          | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `sonar`                 | `JSX`        | `src/example.jsx`         | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `sonar`                 | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `sonar`                 | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `sonar`                 | `Next pages` | `pages/index.jsx`         | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `sonar`                 | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `sonar`                 | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `javascript-with-sonar` | `JavaScript` | `src/example.js`          | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `javascript-with-sonar` | `JSX`        | `src/example.jsx`         | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `javascript-with-sonar` | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `javascript-with-sonar` | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `javascript-with-sonar` | `Next pages` | `pages/index.jsx`         | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `javascript-with-sonar` | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `javascript-with-sonar` | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `all-options`           | `JavaScript` | `src/example.js`          | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `all-options`           | `JSX`        | `src/example.jsx`         | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `all-options`           | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `all-options`           | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `all-options`           | `Next pages` | `pages/index.jsx`         | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `all-options`           | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |
| `all-options`           | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"permissions":["geolocation"]}]` |

### Configured Entries Before ESLint Resolves Overrides

| Scenario                | Severity   | Value     | Config block |
| ----------------------- | ---------- | --------- | ------------ |
| `sonar`                 | 🚨 `error` | `"error"` | `0`          |
| `javascript-with-sonar` | 🚨 `error` | `"error"` | `3`          |
| `all-options`           | 🚨 `error` | `"error"` | `39`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```js filename="bad-example.js" copy
const payload = request.body;
processPayload(payload);
```

  </Tabs.Tab>
  <Tabs.Tab>

```js filename="good-example.js" copy
const payload = schema.parse(request.body);
processPayload(payload);
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/sonar/no-intrusive-permissions`
- **Documentation route:** `/docs/eslint-config/rules/sonar/no-intrusive-permissions`
