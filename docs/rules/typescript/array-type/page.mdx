import { Callout, Tabs } from "nextra/components";

# `@elsikora/typescript/array-type`

<Callout type="error" emoji="🚨">
	**Error.** ElsiKora reports this rule as an error in at least one preset scenario.
</Callout>

## Rule Overview

Require consistently using either `T[]` or `Array<T>` for arrays.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value        |
| ------------------ | ------------ |
| Type               | `suggestion` |
| Fixable            | ✅ Yes       |
| Has suggestions    | ❌ No        |
| Deprecated         | ❌ No        |
| ESLint recommended | `stylistic`  |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
[
	{
		"type": "object",
		"$defs": {
			"arrayOption": {
				"type": "string",
				"enum": ["array", "generic", "array-simple"]
			}
		},
		"additionalProperties": false,
		"properties": {
			"default": {
				"$ref": "#/items/0/$defs/arrayOption",
				"description": "The array type expected for mutable cases."
			},
			"readonly": {
				"$ref": "#/items/0/$defs/arrayOption",
				"description": "The array type expected for readonly cases. If omitted, the value for `default` will be used."
			}
		}
	}
]
```

Upstream default options:

```json filename="default-options.json" copy
[
	{
		"default": "array"
	}
]
```

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                           |
| ---------------- | ------------------------------- |
| Default scenario | `all-options`                   |
| Default target   | `TypeScript` (`src/example.ts`) |
| Default severity | 🚨 `error`                      |
| Default value    | `[2,{"default":"generic"}]`     |

### Effective Result by File Type

| Scenario                         | Target       | File path                 | Effective severity | Effective value             |
| -------------------------------- | ------------ | ------------------------- | ------------------ | --------------------------- |
| `typescript`                     | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript`                     | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript`                     | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript`                     | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-strict`              | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-strict`              | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-strict`              | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-strict`              | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-with-unicorn`        | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-with-unicorn`        | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-with-unicorn`        | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-with-unicorn`        | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-strict-with-unicorn` | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-strict-with-unicorn` | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-strict-with-unicorn` | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `typescript-strict-with-unicorn` | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `all-options`                    | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `all-options`                    | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `all-options`                    | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"default":"generic"}]` |
| `all-options`                    | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"default":"generic"}]` |

### Configured Entries Before ESLint Resolves Overrides

| Scenario                         | Severity   | Value                             | Config block |
| -------------------------------- | ---------- | --------------------------------- | ------------ |
| `typescript`                     | 🚨 `error` | `"error"`                         | `8`          |
| `typescript`                     | 🚨 `error` | `["error",{"default":"generic"}]` | `9`          |
| `typescript-strict`              | 🚨 `error` | `"error"`                         | `8`          |
| `typescript-strict`              | 🚨 `error` | `["error",{"default":"generic"}]` | `9`          |
| `typescript-with-unicorn`        | 🚨 `error` | `"error"`                         | `8`          |
| `typescript-with-unicorn`        | 🚨 `error` | `["error",{"default":"generic"}]` | `9`          |
| `typescript-strict-with-unicorn` | 🚨 `error` | `"error"`                         | `8`          |
| `typescript-strict-with-unicorn` | 🚨 `error` | `["error",{"default":"generic"}]` | `9`          |
| `all-options`                    | 🚨 `error` | `"error"`                         | `63`         |
| `all-options`                    | 🚨 `error` | `["error",{"default":"generic"}]` | `64`         |
| `all-options`                    | 🚨 `error` | `"error"`                         | `73`         |
| `all-options`                    | 🚨 `error` | `["error",{"default":"generic"}]` | `74`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```ts filename="bad-example.ts" copy
type UserMap = {
	[key: string]: User;
};

export type { UserMap };
```

  </Tabs.Tab>
  <Tabs.Tab>

```ts filename="good-example.ts" copy
type UserMap = Record<string, User>;

export type { UserMap };
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/typescript/array-type`
- **Documentation route:** `/docs/eslint-config/rules/typescript/array-type`
