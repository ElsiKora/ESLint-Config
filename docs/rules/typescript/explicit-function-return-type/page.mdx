import { Callout, Tabs } from "nextra/components";

# `@elsikora/typescript/explicit-function-return-type`

<Callout type="error" emoji="🚨">
	**Error.** ElsiKora reports this rule as an error in at least one preset scenario.
</Callout>

## Rule Overview

Require explicit return types on functions and class methods.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value     |
| ------------------ | --------- |
| Type               | `problem` |
| Fixable            | ❌ No     |
| Has suggestions    | ❌ No     |
| Deprecated         | ❌ No     |
| ESLint recommended | `unknown` |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
[
	{
		"type": "object",
		"additionalProperties": false,
		"properties": {
			"allowConciseArrowFunctionExpressionsStartingWithVoid": {
				"type": "boolean",
				"description": "Whether to allow arrow functions that start with the `void` keyword."
			},
			"allowDirectConstAssertionInArrowFunctions": {
				"type": "boolean",
				"description": "Whether to ignore arrow functions immediately returning a `as const` value."
			},
			"allowedNames": {
				"type": "array",
				"description": "An array of function/method names that will not have their arguments or return values checked.",
				"items": {
					"type": "string"
				}
			},
			"allowExpressions": {
				"type": "boolean",
				"description": "Whether to ignore function expressions (functions which are not part of a declaration)."
			},
			"allowFunctionsWithoutTypeParameters": {
				"type": "boolean",
				"description": "Whether to ignore functions that don't have generic type parameters."
			},
			"allowHigherOrderFunctions": {
				"type": "boolean",
				"description": "Whether to ignore functions immediately returning another function expression."
			},
			"allowIIFEs": {
				"type": "boolean",
				"description": "Whether to ignore immediately invoked function expressions (IIFEs)."
			},
			"allowTypedFunctionExpressions": {
				"type": "boolean",
				"description": "Whether to ignore type annotations on the variable of function expressions."
			}
		}
	}
]
```

Upstream default options:

```json filename="default-options.json" copy
[
	{
		"allowConciseArrowFunctionExpressionsStartingWithVoid": false,
		"allowDirectConstAssertionInArrowFunctions": true,
		"allowedNames": [],
		"allowExpressions": false,
		"allowFunctionsWithoutTypeParameters": false,
		"allowHigherOrderFunctions": true,
		"allowIIFEs": false,
		"allowTypedFunctionExpressions": true
	}
]
```

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                           |
| ---------------- | ------------------------------- |
| Default scenario | `all-options`                   |
| Default target   | `TypeScript` (`src/example.ts`) |
| Default severity | 🚨 `error`                      |
| Default value    | `[2]`                           |

### Effective Result by File Type

| Scenario                         | Target       | File path                 | Effective severity | Effective value |
| -------------------------------- | ------------ | ------------------------- | ------------------ | --------------- |
| `typescript-strict`              | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2]`           |
| `typescript-strict`              | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2]`           |
| `typescript-strict`              | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2]`           |
| `typescript-strict`              | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2]`           |
| `typescript-strict-with-unicorn` | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2]`           |
| `typescript-strict-with-unicorn` | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2]`           |
| `typescript-strict-with-unicorn` | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2]`           |
| `typescript-strict-with-unicorn` | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2]`           |
| `all-options`                    | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2]`           |
| `all-options`                    | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2]`           |
| `all-options`                    | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2]`           |
| `all-options`                    | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2]`           |

### Configured Entries Before ESLint Resolves Overrides

| Scenario                         | Severity   | Value     | Config block |
| -------------------------------- | ---------- | --------- | ------------ |
| `typescript-strict`              | 🚨 `error` | `"error"` | `9`          |
| `typescript-strict-with-unicorn` | 🚨 `error` | `"error"` | `9`          |
| `all-options`                    | 🚨 `error` | `"error"` | `74`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```ts filename="bad-example.ts" copy
type UserMap = {
	[key: string]: User;
};

export type { UserMap };
```

  </Tabs.Tab>
  <Tabs.Tab>

```ts filename="good-example.ts" copy
type UserMap = Record<string, User>;

export type { UserMap };
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/typescript/explicit-function-return-type`
- **Documentation route:** `/docs/eslint-config/rules/typescript/explicit-function-return-type`
