import { Callout, Tabs } from "nextra/components";

# `@elsikora/nest/2/api-methods-should-be-guarded`

<Callout type="info" emoji="🧭">
	**Disabled.** ElsiKora includes this rule as an intentional disabled override in at least one preset scenario.
</Callout>

## Rule Overview

Endpoints should have authentication guards to maintain our security model.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value        |
| ------------------ | ------------ |
| Type               | `suggestion` |
| Fixable            | ❌ No        |
| Has suggestions    | ❌ No        |
| Deprecated         | ❌ No        |
| ESLint recommended | `unknown`    |

## All Supported Options

This rule does not expose configurable options through its ESLint metadata schema.

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                           |
| ---------------- | ------------------------------- |
| Default scenario | `all-options`                   |
| Default target   | `TypeScript` (`src/example.ts`) |
| Default severity | 🧭 `off`                        |
| Default value    | `[0]`                           |

### Effective Result by File Type

| Scenario      | Target       | File path        | Effective severity | Effective value |
| ------------- | ------------ | ---------------- | ------------------ | --------------- |
| `nest`        | `TypeScript` | `src/example.ts` | 🧭 `off`           | `[0]`           |
| `all-options` | `TypeScript` | `src/example.ts` | 🧭 `off`           | `[0]`           |

### Configured Entries Before ESLint Resolves Overrides

| Scenario      | Severity | Value   | Config block |
| ------------- | -------- | ------- | ------------ |
| `nest`        | 🧭 `off` | `"off"` | `0`          |
| `all-options` | 🧭 `off` | `"off"` | `20`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```ts filename="bad-example.ts" copy
@Get(":id")
findOne(@Param("id") id: string) {
	return this.users.findOne(id);
}
```

  </Tabs.Tab>
  <Tabs.Tab>

```ts filename="good-example.ts" copy
@ApiOperation({ summary: "Get a user" })
@ApiOkResponse({ type: UserDto })
@Get(":id")
findOne(@Param("id") id: string) {
	return this.users.findOne(id);
}
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/nest/2/api-methods-should-be-guarded`
- **Documentation route:** `/docs/eslint-config/rules/nest/2/api-methods-should-be-guarded`
