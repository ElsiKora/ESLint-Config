import { Callout, Tabs } from "nextra/components";

# `@elsikora/typescript/no-empty-function`

<Callout type="error" emoji="🚨">
	**Error.** ElsiKora reports this rule as an error in at least one preset scenario.
</Callout>

## Rule Overview

Disallow empty functions.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value        |
| ------------------ | ------------ |
| Type               | `suggestion` |
| Fixable            | ❌ No        |
| Has suggestions    | ✅ Yes       |
| Deprecated         | ❌ No        |
| ESLint recommended | `stylistic`  |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
[
	{
		"type": "object",
		"properties": {
			"allow": {
				"type": "array",
				"items": {
					"enum": ["functions", "arrowFunctions", "generatorFunctions", "methods", "generatorMethods", "getters", "setters", "constructors", "private-constructors", "protected-constructors", "asyncFunctions", "asyncMethods", "decoratedFunctions", "overrideMethods"],
					"type": "string"
				},
				"uniqueItems": true,
				"description": "Locations and kinds of functions that are allowed to be empty."
			}
		},
		"additionalProperties": false
	}
]
```

Upstream default options:

```json filename="default-options.json" copy
[
	{
		"allow": []
	}
]
```

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                           |
| ---------------- | ------------------------------- |
| Default scenario | `all-options`                   |
| Default target   | `TypeScript` (`src/example.ts`) |
| Default severity | 🚨 `error`                      |
| Default value    | `[2,{"allow":[]}]`              |

### Effective Result by File Type

| Scenario                         | Target       | File path                 | Effective severity | Effective value    |
| -------------------------------- | ------------ | ------------------------- | ------------------ | ------------------ |
| `typescript`                     | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript`                     | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript`                     | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript`                     | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-strict`              | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-strict`              | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-strict`              | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-strict`              | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-with-unicorn`        | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-with-unicorn`        | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-with-unicorn`        | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-with-unicorn`        | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-strict-with-unicorn` | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-strict-with-unicorn` | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-strict-with-unicorn` | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"allow":[]}]` |
| `typescript-strict-with-unicorn` | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"allow":[]}]` |
| `all-options`                    | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2,{"allow":[]}]` |
| `all-options`                    | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2,{"allow":[]}]` |
| `all-options`                    | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2,{"allow":[]}]` |
| `all-options`                    | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"allow":[]}]` |

### Configured Entries Before ESLint Resolves Overrides

| Scenario                         | Severity   | Value     | Config block |
| -------------------------------- | ---------- | --------- | ------------ |
| `typescript`                     | 🚨 `error` | `"error"` | `8`          |
| `typescript`                     | 🚨 `error` | `"error"` | `9`          |
| `typescript-strict`              | 🚨 `error` | `"error"` | `8`          |
| `typescript-strict`              | 🚨 `error` | `"error"` | `9`          |
| `typescript-with-unicorn`        | 🚨 `error` | `"error"` | `8`          |
| `typescript-with-unicorn`        | 🚨 `error` | `"error"` | `9`          |
| `typescript-strict-with-unicorn` | 🚨 `error` | `"error"` | `8`          |
| `typescript-strict-with-unicorn` | 🚨 `error` | `"error"` | `9`          |
| `all-options`                    | 🚨 `error` | `"error"` | `63`         |
| `all-options`                    | 🚨 `error` | `"error"` | `64`         |
| `all-options`                    | 🚨 `error` | `"error"` | `73`         |
| `all-options`                    | 🚨 `error` | `"error"` | `74`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```ts filename="bad-example.ts" copy
class UserService {
	constructor(private readonly repository: UserRepository) {}

	load(id: string): Promise<User> {
		return this.repository.findById(id);
	}
}

export { UserService };
```

  </Tabs.Tab>
  <Tabs.Tab>

```ts filename="good-example.ts" copy
class UserService {
	constructor(private readonly repository: UserRepository) {}

	loadUser(id: string): Promise<User> {
		return this.repository.findById(id);
	}
}

export { UserService };
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/typescript/no-empty-function`
- **Documentation route:** `/docs/eslint-config/rules/typescript/no-empty-function`
