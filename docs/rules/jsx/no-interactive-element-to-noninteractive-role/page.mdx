import { Callout, Tabs } from "nextra/components";

# `@elsikora/jsx/no-interactive-element-to-noninteractive-role`

<Callout type="error" emoji="🚨">
	**Error.** ElsiKora reports this rule as an error in at least one preset scenario.
</Callout>

## Rule Overview

Disallow interactive elements being assigned non-interactive roles.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value     |
| ------------------ | --------- |
| Type               | `unknown` |
| Fixable            | ❌ No     |
| Has suggestions    | ❌ No     |
| Deprecated         | ❌ No     |
| ESLint recommended | `unknown` |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
[
	{
		"type": "object",
		"additionalProperties": {
			"type": "array",
			"items": {
				"type": "string"
			},
			"uniqueItems": true
		},
		"description": "An object mapping element types to arrays of non-interactive roles that are allowed for those elements."
	}
]
```

No upstream default options are exposed in the rule metadata.

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                                                 |
| ---------------- | ----------------------------------------------------- |
| Default scenario | `all-options`                                         |
| Default target   | `JavaScript` (`src/example.js`)                       |
| Default severity | 🚨 `error`                                            |
| Default value    | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |

### Effective Result by File Type

| Scenario      | Target         | File path                 | Effective severity | Effective value                                       |
| ------------- | -------------- | ------------------------- | ------------------ | ----------------------------------------------------- |
| `jsx`         | `JavaScript`   | `src/example.js`          | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `jsx`         | `JSX`          | `src/example.jsx`         | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `jsx`         | `TSX`          | `src/example.tsx`         | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `jsx`         | `Next pages`   | `pages/index.jsx`         | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `jsx`         | `Next app`     | `app/page.tsx`            | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `jsx`         | `Storybook`    | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `JavaScript`   | `src/example.js`          | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `JSX`          | `src/example.jsx`         | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `TypeScript`   | `src/example.ts`          | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `TSX`          | `src/example.tsx`         | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `Next pages`   | `pages/index.jsx`         | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `Next app`     | `app/page.tsx`            | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `Storybook`    | `src/example.stories.tsx` | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `Package JSON` | `package.json`            | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `JSON`         | `config.json`             | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `YAML`         | `config.yaml`             | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `Markdown`     | `README.md`               | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |
| `all-options` | `CSS`          | `src/styles.css`          | 🚨 `error`         | `[2,{"tr":["none","presentation"],"canvas":["img"]}]` |

### Configured Entries Before ESLint Resolves Overrides

| Scenario      | Severity   | Value                                                       | Config block |
| ------------- | ---------- | ----------------------------------------------------------- | ------------ |
| `jsx`         | 🚨 `error` | `["error",{"tr":["none","presentation"],"canvas":["img"]}]` | `0`          |
| `all-options` | 🚨 `error` | `["error",{"tr":["none","presentation"],"canvas":["img"]}]` | `17`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```tsx filename="bad-example.tsx" copy
export function VolumeSlider() {
	return (
		<div role="slider" aria-valuenow="loud">
			Volume
		</div>
	);
}
```

  </Tabs.Tab>
  <Tabs.Tab>

```tsx filename="good-example.tsx" copy
export function VolumeSlider() {
	return (
		<div role="slider" aria-valuemin={0} aria-valuemax={100} aria-valuenow={60} tabIndex={0}>
			Volume
		</div>
	);
}
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/jsx/no-interactive-element-to-noninteractive-role`
- **Documentation route:** `/docs/eslint-config/rules/jsx/no-interactive-element-to-noninteractive-role`
