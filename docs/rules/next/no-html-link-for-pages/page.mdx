import { Callout, Tabs } from "nextra/components";

# `@elsikora/next/no-html-link-for-pages`

<Callout type="warning" emoji="⚠️">
	**Warning.** ElsiKora reports this rule as a warning in at least one preset scenario.
</Callout>

## Rule Overview

Prevent usage of `<a>` elements to navigate to internal Next.js pages.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value     |
| ------------------ | --------- |
| Type               | `problem` |
| Fixable            | ❌ No     |
| Has suggestions    | ❌ No     |
| Deprecated         | ❌ No     |
| ESLint recommended | `true`    |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
[
	{
		"oneOf": [
			{
				"type": "string"
			},
			{
				"type": "array",
				"uniqueItems": true,
				"items": {
					"type": "string"
				}
			}
		]
	}
]
```

No upstream default options are exposed in the rule metadata.

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                           |
| ---------------- | ------------------------------- |
| Default scenario | `all-options`                   |
| Default target   | `JavaScript` (`src/example.js`) |
| Default severity | ⚠️ `warn`                       |
| Default value    | `[1]`                           |

### Effective Result by File Type

| Scenario      | Target       | File path                 | Effective severity | Effective value |
| ------------- | ------------ | ------------------------- | ------------------ | --------------- |
| `next`        | `JavaScript` | `src/example.js`          | ⚠️ `warn`          | `[1]`           |
| `next`        | `JSX`        | `src/example.jsx`         | ⚠️ `warn`          | `[1]`           |
| `next`        | `TypeScript` | `src/example.ts`          | ⚠️ `warn`          | `[1]`           |
| `next`        | `TSX`        | `src/example.tsx`         | ⚠️ `warn`          | `[1]`           |
| `next`        | `Next pages` | `pages/index.jsx`         | ⚠️ `warn`          | `[1]`           |
| `next`        | `Next app`   | `app/page.tsx`            | ⚠️ `warn`          | `[1]`           |
| `next`        | `Storybook`  | `src/example.stories.tsx` | ⚠️ `warn`          | `[1]`           |
| `all-options` | `JavaScript` | `src/example.js`          | ⚠️ `warn`          | `[1]`           |
| `all-options` | `JSX`        | `src/example.jsx`         | ⚠️ `warn`          | `[1]`           |
| `all-options` | `TypeScript` | `src/example.ts`          | ⚠️ `warn`          | `[1]`           |
| `all-options` | `TSX`        | `src/example.tsx`         | ⚠️ `warn`          | `[1]`           |
| `all-options` | `Next pages` | `pages/index.jsx`         | ⚠️ `warn`          | `[1]`           |
| `all-options` | `Next app`   | `app/page.tsx`            | ⚠️ `warn`          | `[1]`           |
| `all-options` | `Storybook`  | `src/example.stories.tsx` | ⚠️ `warn`          | `[1]`           |

### Configured Entries Before ESLint Resolves Overrides

| Scenario      | Severity  | Value    | Config block |
| ------------- | --------- | -------- | ------------ |
| `next`        | ⚠️ `warn` | `"warn"` | `2`          |
| `all-options` | ⚠️ `warn` | `"warn"` | `23`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```tsx filename="bad-example.tsx" copy
export function AccountNavigation() {
	return <a href="/account">Account</a>;
}
```

  </Tabs.Tab>
  <Tabs.Tab>

```tsx filename="good-example.tsx" copy
import Link from "next/link";

export function AccountNavigation() {
	return <Link href="/account">Account</Link>;
}
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/next/no-html-link-for-pages`
- **Documentation route:** `/docs/eslint-config/rules/next/no-html-link-for-pages`
