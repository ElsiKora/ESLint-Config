import { Callout, Tabs } from "nextra/components";

# `@elsikora/node/no-missing-import`

<Callout type="info" emoji="🧭">
	**Disabled.** ElsiKora includes this rule as an intentional disabled override in at least one preset scenario.
</Callout>

## Rule Overview

Disallow `import` declarations which import missing modules.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value     |
| ------------------ | --------- |
| Type               | `problem` |
| Fixable            | ❌ No     |
| Has suggestions    | ❌ No     |
| Deprecated         | ❌ No     |
| ESLint recommended | `true`    |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
[
	{
		"type": "object",
		"properties": {
			"allowModules": {
				"type": "array",
				"items": {
					"type": "string",
					"pattern": "^(?:virtual:)?(?:@[a-zA-Z0-9_\\-.]+/)?[a-zA-Z0-9_\\-.]+$"
				},
				"uniqueItems": true
			},
			"resolvePaths": {
				"type": "array",
				"items": {
					"type": "string"
				},
				"uniqueItems": true
			},
			"resolverConfig": {
				"type": "object",
				"properties": {},
				"additionalProperties": true
			},
			"tryExtensions": {
				"type": "array",
				"items": {
					"type": "string",
					"pattern": "^\\."
				},
				"uniqueItems": true
			},
			"ignoreTypeImport": {
				"type": "boolean",
				"default": false
			},
			"tsconfigPath": {
				"type": "string"
			},
			"typescriptExtensionMap": {
				"oneOf": [
					{
						"type": "array",
						"items": {
							"type": "array",
							"prefixItems": [
								{
									"type": "string",
									"pattern": "^(?:|\\.\\w+)$"
								},
								{
									"type": "string",
									"pattern": "^\\.\\w+$"
								}
							],
							"additionalItems": false
						},
						"uniqueItems": true
					},
					{
						"type": "string",
						"enum": ["react", "react-jsx", "react-jsxdev", "react-native", "preserve"]
					}
				]
			}
		},
		"additionalProperties": false
	}
]
```

No upstream default options are exposed in the rule metadata.

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                           |
| ---------------- | ------------------------------- |
| Default scenario | `all-options`                   |
| Default target   | `JavaScript` (`src/example.js`) |
| Default severity | 🧭 `off`                        |
| Default value    | `[0]`                           |

### Effective Result by File Type

| Scenario            | Target       | File path                 | Effective severity | Effective value |
| ------------------- | ------------ | ------------------------- | ------------------ | --------------- |
| `node`              | `JavaScript` | `src/example.js`          | 🧭 `off`           | `[0]`           |
| `node`              | `JSX`        | `src/example.jsx`         | 🧭 `off`           | `[0]`           |
| `node`              | `TypeScript` | `src/example.ts`          | 🧭 `off`           | `[0]`           |
| `node`              | `TSX`        | `src/example.tsx`         | 🧭 `off`           | `[0]`           |
| `node`              | `Next pages` | `pages/index.jsx`         | 🧭 `off`           | `[0]`           |
| `node`              | `Next app`   | `app/page.tsx`            | 🧭 `off`           | `[0]`           |
| `node`              | `Storybook`  | `src/example.stories.tsx` | 🧭 `off`           | `[0]`           |
| `node-with-unicorn` | `JavaScript` | `src/example.js`          | 🧭 `off`           | `[0]`           |
| `node-with-unicorn` | `JSX`        | `src/example.jsx`         | 🧭 `off`           | `[0]`           |
| `node-with-unicorn` | `TypeScript` | `src/example.ts`          | 🧭 `off`           | `[0]`           |
| `node-with-unicorn` | `TSX`        | `src/example.tsx`         | 🧭 `off`           | `[0]`           |
| `node-with-unicorn` | `Next pages` | `pages/index.jsx`         | 🧭 `off`           | `[0]`           |
| `node-with-unicorn` | `Next app`   | `app/page.tsx`            | 🧭 `off`           | `[0]`           |
| `node-with-unicorn` | `Storybook`  | `src/example.stories.tsx` | 🧭 `off`           | `[0]`           |
| `all-options`       | `JavaScript` | `src/example.js`          | 🧭 `off`           | `[0]`           |
| `all-options`       | `JSX`        | `src/example.jsx`         | 🧭 `off`           | `[0]`           |
| `all-options`       | `TypeScript` | `src/example.ts`          | 🧭 `off`           | `[0]`           |
| `all-options`       | `TSX`        | `src/example.tsx`         | 🧭 `off`           | `[0]`           |
| `all-options`       | `Next pages` | `pages/index.jsx`         | 🧭 `off`           | `[0]`           |
| `all-options`       | `Next app`   | `app/page.tsx`            | 🧭 `off`           | `[0]`           |
| `all-options`       | `Storybook`  | `src/example.stories.tsx` | 🧭 `off`           | `[0]`           |

### Configured Entries Before ESLint Resolves Overrides

| Scenario            | Severity   | Value     | Config block |
| ------------------- | ---------- | --------- | ------------ |
| `node`              | 🚨 `error` | `"error"` | `0`          |
| `node`              | 🧭 `off`   | `"off"`   | `1`          |
| `node-with-unicorn` | 🚨 `error` | `"error"` | `0`          |
| `node-with-unicorn` | 🧭 `off`   | `"off"`   | `1`          |
| `all-options`       | 🚨 `error` | `"error"` | `24`         |
| `all-options`       | 🧭 `off`   | `"off"`   | `25`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```js filename="bad-example.js" copy
import helper from "../scripts/local-helper.js";

helper();
```

  </Tabs.Tab>
  <Tabs.Tab>

```js filename="good-example.js" copy
import { createLogger } from "./logger.js";

createLogger().info("ready");
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/node/no-missing-import`
- **Documentation route:** `/docs/eslint-config/rules/node/no-missing-import`
