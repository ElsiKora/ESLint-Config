import { Callout, Tabs } from "nextra/components";

# `@elsikora/node/no-unpublished-require`

<Callout type="error" emoji="🚨">
	**Error.** ElsiKora reports this rule as an error in at least one preset scenario.
</Callout>

## Rule Overview

Disallow `require()` expressions which import private modules.

This page describes the rule in general first, then shows the exact ElsiKora preset configuration separately.

## Rule Metadata

| Property           | Value     |
| ------------------ | --------- |
| Type               | `problem` |
| Fixable            | ❌ No     |
| Has suggestions    | ❌ No     |
| Deprecated         | ❌ No     |
| ESLint recommended | `true`    |

## All Supported Options

The rule exposes the following ESLint option schema:

```json filename="options-schema.json" copy
[
	{
		"type": "object",
		"properties": {
			"allowModules": {
				"type": "array",
				"items": {
					"type": "string",
					"pattern": "^(?:virtual:)?(?:@[a-zA-Z0-9_\\-.]+/)?[a-zA-Z0-9_\\-.]+$"
				},
				"uniqueItems": true
			},
			"convertPath": {
				"anyOf": [
					{
						"type": "object",
						"properties": {},
						"patternProperties": {
							"^.+$": {
								"type": "array",
								"items": {
									"type": "string"
								},
								"minItems": 2,
								"maxItems": 2
							}
						},
						"additionalProperties": false
					},
					{
						"type": "array",
						"items": {
							"type": "object",
							"properties": {
								"include": {
									"type": "array",
									"items": {
										"type": "string"
									},
									"minItems": 1,
									"uniqueItems": true
								},
								"exclude": {
									"type": "array",
									"items": {
										"type": "string"
									},
									"uniqueItems": true
								},
								"replace": {
									"type": "array",
									"items": {
										"type": "string"
									},
									"minItems": 2,
									"maxItems": 2
								}
							},
							"additionalProperties": false,
							"required": ["include", "replace"]
						},
						"minItems": 1
					}
				]
			},
			"resolvePaths": {
				"type": "array",
				"items": {
					"type": "string"
				},
				"uniqueItems": true
			},
			"resolverConfig": {
				"type": "object",
				"properties": {},
				"additionalProperties": true
			},
			"tryExtensions": {
				"type": "array",
				"items": {
					"type": "string",
					"pattern": "^\\."
				},
				"uniqueItems": true
			},
			"ignorePrivate": {
				"type": "boolean",
				"default": true
			}
		},
		"additionalProperties": false
	}
]
```

No upstream default options are exposed in the rule metadata.

## ElsiKora Default Setting

The default shown here is the first effective result produced by ESLint's own config resolver. Raw configured entries are listed separately so overrides from React, Next.js, Prettier, and other presets stay visible.

| Field            | Value                           |
| ---------------- | ------------------------------- |
| Default scenario | `all-options`                   |
| Default target   | `JavaScript` (`src/example.js`) |
| Default severity | 🚨 `error`                      |
| Default value    | `[2]`                           |

### Effective Result by File Type

| Scenario            | Target       | File path                 | Effective severity | Effective value |
| ------------------- | ------------ | ------------------------- | ------------------ | --------------- |
| `node`              | `JavaScript` | `src/example.js`          | 🚨 `error`         | `[2]`           |
| `node`              | `JSX`        | `src/example.jsx`         | 🚨 `error`         | `[2]`           |
| `node`              | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2]`           |
| `node`              | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2]`           |
| `node`              | `Next pages` | `pages/index.jsx`         | 🚨 `error`         | `[2]`           |
| `node`              | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2]`           |
| `node`              | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2]`           |
| `node-with-unicorn` | `JavaScript` | `src/example.js`          | 🚨 `error`         | `[2]`           |
| `node-with-unicorn` | `JSX`        | `src/example.jsx`         | 🚨 `error`         | `[2]`           |
| `node-with-unicorn` | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2]`           |
| `node-with-unicorn` | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2]`           |
| `node-with-unicorn` | `Next pages` | `pages/index.jsx`         | 🚨 `error`         | `[2]`           |
| `node-with-unicorn` | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2]`           |
| `node-with-unicorn` | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2]`           |
| `all-options`       | `JavaScript` | `src/example.js`          | 🚨 `error`         | `[2]`           |
| `all-options`       | `JSX`        | `src/example.jsx`         | 🚨 `error`         | `[2]`           |
| `all-options`       | `TypeScript` | `src/example.ts`          | 🚨 `error`         | `[2]`           |
| `all-options`       | `TSX`        | `src/example.tsx`         | 🚨 `error`         | `[2]`           |
| `all-options`       | `Next pages` | `pages/index.jsx`         | 🚨 `error`         | `[2]`           |
| `all-options`       | `Next app`   | `app/page.tsx`            | 🚨 `error`         | `[2]`           |
| `all-options`       | `Storybook`  | `src/example.stories.tsx` | 🚨 `error`         | `[2]`           |

### Configured Entries Before ESLint Resolves Overrides

| Scenario            | Severity   | Value     | Config block |
| ------------------- | ---------- | --------- | ------------ |
| `node`              | 🚨 `error` | `"error"` | `0`          |
| `node-with-unicorn` | 🚨 `error` | `"error"` | `0`          |
| `all-options`       | 🚨 `error` | `"error"` | `24`         |

## Examples

<Tabs items={["❌ Avoid", "✅ Prefer"]}>
  <Tabs.Tab>

```js filename="bad-example.js" copy
import helper from "../scripts/local-helper.js";

helper();
```

  </Tabs.Tab>
  <Tabs.Tab>

```js filename="good-example.js" copy
import { createLogger } from "./logger.js";

createLogger().info("ready");
```

  </Tabs.Tab>
</Tabs>

## Related Reference

- **Final rule id:** `@elsikora/node/no-unpublished-require`
- **Documentation route:** `/docs/eslint-config/rules/node/no-unpublished-require`
